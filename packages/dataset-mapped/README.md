# Mapped Dataset

The mapped-dataset provides a mapping layer between the data requirements of individual components to the
actual layout of source datasets. This is usually used when there is a 'mapping' specification defined on a component.
